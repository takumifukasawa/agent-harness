# references — 外部知識の持ち込み場所

エージェントは実行中に文脈へ入らないものを知らない。依存ライブラリの仕様、外部 API の契約、社内の設計規約など、判断の根拠にした外部知識はここに抜粋して置く（`<name>-llms.txt` や Markdown）。

| ファイル | 元 | 取得日 | 何に使うか |
|---|---|---|---|

- 元 URL と取得日を必ず書く（腐敗の検知に使う）。
- 全文コピーではなく、このプロジェクトが依存している部分の抜粋にする。
