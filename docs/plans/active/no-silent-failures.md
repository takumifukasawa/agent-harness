# no-silent-failures — 黙って通るものを機械で止める

- 開始: 2026-09-21
- 状態: 進行中
- 関連: [spec](../../spec/no-silent-failures.md), `docs/tech-debt.md` #14 #15

## 目的（何ができれば完了か）

「失敗しているのに誰も気づけない」形の不具合を 3 つまとめて塞ぐ。**壊れているのと正常なのが出力から区別できない**状態を無くす。

## 受け入れ条件（検査で確認できる形に）

[spec](../../spec/no-silent-failures.md) の A / B / C。**3 つとも「回帰を止める検査」を条件に含めている**（この題材の性質上、検査が無ければ同じことが再発する）。

## タスク分解（依存順）

| # | タスク | 状態 | 備考 |
|---|---|---|---|
| T01 | `hash_server_start` が stderr を握りつぶすのをやめる（#15） | **done** | `exec` の落とし穴。`init`/`update`/`status`/`diff` の全経路に効く |
| T02 | `managed` の case 衝突で `doctor` が偽の緑を返すのを止める（#14） | 進行中 | T01 と `bin/harness` / CHANGELOG が競合するので逐次 |
| T03 | `gc` に「計画のタスク表と状態欄の矛盾」を足す（C） | **done** | 統括を縛る仕組み。T01 と並列（触るのは `gc.sh` と `tests/gc.sh` だけ） |

## 決定ログ（日付・決めたこと・理由・落選案）

- **2026-09-21 / C が見るのは「タスク表と状態欄の矛盾」だけ。** 判定が機械的で誤検知が少ない。落選: spec との食い違いや参照切れも見る（表記揺れでの誤検知が増え、gc の既存項目を埋もれさせる）、人間向けチェックリストだけ（今回もスキルに書いてあって漏れた。文章では止まらない）
- **2026-09-21 / A3 は「気づけるだけ」。** 自動ロールバックはしない。失敗の種類によっては消してはいけないし、既存ファイルを誤って消すリスクを背負う
- **2026-09-21 / この題材は「統括自身の抜け」から生まれた。** 2026-09-21 の onboarding-polish の最終レビューで、統括の書き戻し漏れが 2 件拾われた。実装役は `files_scope` と検査で縛れるのに、統括を縛るものが無いという非対称に気づいたのが C の発端

## 進捗ログ（セッションごとに 1〜3 行）

- **2026-09-21（起票・分解）**: 前の題材（onboarding-polish）の最終レビューで出た #14 / #15 と、統括の書き戻し漏れ 2 件を 1 つの題材にまとめた。共通するのは「黙って通るから気づけない」こと。未確定 2 件をユーザーと合意し、3 タスクに分解。T01 と T03 を並列で起動した。
- **2026-09-21（T01 / T03 完了）**: T01 は `hash_server_start` だけでなく **`hash_server_stop` にも同じ bare `exec` のバグ**を見つけて両方修正（`{ exec ...; } 2>/dev/null` に包み、fd2 のリダイレクトをブロック内に限定。fd3/4 の常駐設計は維持）。実機で「修正前は stderr 0 バイト → 修正後は `mkdir: ... File exists` が出る」を確認し、**高速化（CS-B1）も無傷**（init 0.94→0.93s / update 0.81→0.77s）。T03 は `gc` に項目 10 を追加し、TDD の途中で列インデックスの off-by-one を自分で発見・修正。検査は 21 → **22 件**、`gc` のシナリオは 7 → **10 件**。
  **統括の判断ミスが 1 件**: T01 と T03 を「触るファイルが重ならない」という理由で並列にしたが、**`bash bin/harness update` は `files_scope` を超えて正本全体を同期する**ため、T03 の update が T01 の未コミット変更を巻き込んだ（T03 側が手で修復）。`docs/learnings.md` に記録した。**並列可否は「触るファイルが重なるか」ではなく「全体同期コマンドを含むか」で判断する。**
