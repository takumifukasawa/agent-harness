# tech-debt — 既知の負債

<!-- 負債は高金利のローン。溜めて一括返済するより、小さく継続的に返す。
     `harness gc` はここに載っている項目の放置日数を報告する。返済したら行を消さず「返済済 (コミット)」にする。 -->

| # | 内容 | 影響範囲 | 起票日 | 状態 |
|---|---|---|---|---|
| 1 | `.claude/settings.json` の自動マージが `node` 前提。無い環境では断片を手で反映 | Claude アダプタ | 2026-09-17 | 未着手（jq 対応か、bash だけで済む簡易マージを検討） |
| 2 | `harness gc` の判定がヒューリスティック（日付の文字列パース、索引は `(` 前方一致） | gc | 2026-09-17 | 未着手（誤検知が出たら精度を上げる） |
| 3 | macOS で**実機確認した**（2026-09-20、Darwin 24.6 / arm64 / bash 3.2.57 / ja_JP.UTF-8）。`harness check` は pass=9 fail=4。**4 件中 3 件の根本原因は 1 つ**で、`declare -A`（`bin/harness:355`）が `invalid option` で **`init` を即死**させ、テストの setup が全滅していた。走査で挙げていた 4 箇所の実測結果: (a) **bash 4+ 必須は的中**（ただし doctor の B1 は FAIL を出すものの doctor 自身も完走しない）(b) **`sha256sum` は外れ。この機には `/sbin/sha256sum` が、`jq` も `/usr/bin/jq` にあった**（Apple 提供、macOS 15 以降）。古い macOS では依然リスク (c) `date -d` は `illegal option` を確認したが**未到達** (d) `sed -i` の引数必須も確認したが**未到達**。加えて**走査に無かった 5 つ目**: bash 3.2 + UTF-8 では `"$var日本語"` が `unbound variable` になる（**24 行**。`${var}` に括れば通る） | CLI 全体 / テスト | 2026-09-17 | **ほぼ返済済**（T01 + T02、2026-09-20）。`check` は pass=18 fail=0、`doctor` は OK=16 WARN=0 FAIL=0。(c) `date -d` と pre-commit の実行ビットは T02 で解消（#8 参照）。**残るのは古い macOS（15 未満、`sha256sum` / `jq` が無い前提）の経路が未検証**であること |
| 4 | GitHub からの `curl \| bash` init を実機で未確認（`file://` のみ） | 配布 | 2026-09-17 | **返済済**（2026-09-21）。cross-env フェーズ 1（35 コミット）を main へ FF マージして push（`305d57b` → `c1de264`）したうえで、素の使い捨てディレクトリに `bash bin/harness init --source https://github.com/takumifukasawa/agent-harness.git` を 1 回実行し、**new=25 seeded=15 conflicts=0 / `doctor` OK=15 WARN=1 FAIL=0** を実機で確認した。WARN 1 は「`source.local` が無いので新版を確認できない」＝公開 URL から導入した直後の正常な状態（直し方も案内される）。以下は返済前の記録: **機構は確認済**（T02、2026-09-20）。公開 https URL / `curl \| bash` / `file://` の 3 経路を実機で通し、URL 解決（clone→キャッシュ→配置、new=25 seeded=15）は動いた。標準入力経路の `${BASH_SOURCE[0]}` もここで直した。**残る失敗は公開 main が T01 前（305d57b）であること**に由来し、現ブランチ内容の clone 経路（`file://` + `--ref cross-env`）では doctor FAIL 0 / check 全 pass。**T03 の後に main へ載せ、`bash bin/harness init --source https://github.com/takumifukasawa/agent-harness.git` を 1 回回して doctor FAIL 0 を確認すれば閉じられる** |
| 5 | このリポジトリに導入コピー（`.agents/` 等）とペイロード（`harness/`）が同居して二重に見える | 可読性 | 2026-09-17 | 受容（dogfood のため。`docs/architecture.md` に境界を明記） |
| 6 | `tests/doctor.sh` の下限が `doctor` 呼び出し 1 回 ≒ 3 秒（外部コマンドを多数呼ぶ）。T13 でフィクスチャ共有により 6m51s → 2m10s まで縮めたが、目標の 20〜40 秒には `harness/scripts/doctor.sh` 側の最適化が要る | テスト時間 | 2026-09-18 | **返済中**（2026-09-21: ハッシュのバッチ化で 95s → **62s**、続けて B5 の一括判定・`same_script` の重複削減・`apply_plan` の一時ファイル使い回しで **50s**。`doctor` 337→253ms、`update` 829→611ms、`init` 1080→840ms）。**起票時に挙げた回数（`doctor` 13 回）も静的 grep 由来で外れていた（実測 45 回）**。**返済はここで打ち切る**（決定 0008: 残りの候補は効果 2〜4 秒に対し `apply_plan` の復元判断やシナリオの独立性を触る取引になる）。再浮上は `check` が再び 60 秒を超えたとき。**2026-09-21 に全件実測**（`check` が検査ごとの所要秒を出すようにした。`docs/spec/check-speed.md` の A）: フル 95 秒のうち **`tests/doctor.sh` 58s（61%）+ `tests/update.sh` 30s（32%）で 93%**、残り 16 件は合計 7 秒。**`--fast` は 1 秒**。**この負債は `tests/doctor.sh` だけを挙げていたが、`tests/update.sh` の 30 秒は記載が無かった**（測らずに返していたら 3 割取り逃していた）。**この負債の前提は 2 つとも実測で覆った**: (1) 「`doctor` 呼び出し 1 回 ≒ 3 秒」→ **実測 0.6 秒** (2) 「`doctor.sh` 側の最適化が要る」→ `tests/doctor.sh` が呼ぶのは **`doctor` 13 回に対し `init` 15 回・`update` 27 回**で、**`init` / `update` が各 1.7 秒**。つまり時間を食っているのは診断そのものではなく**テストが作る使い捨てプロジェクト**。「外部コマンド数を減らせば 3〜4 倍」も未検証のまま |
| 7 | `source` の解決順のテストが `tests/doctor.sh` に同居している（本来は `tests/source.sh` に分けて `checks.sh` に登録するのが素直）。T09 の `files_scope` の制約による | テスト構成 | 2026-09-18 | **返済済**（2026-09-21）。`tests/source.sh`（4 シナリオ）に分離し `.harness/checks.sh` に `source scenarios` として登録。移動は本文の文字列比較で同一性を確認し、シナリオ総数は 44 = doctor 40 + source 4 で不変、表明も減っていない（285 → 289 行）。**この分離は Codex の実装役に回した**（cross-env の B3 の検証を兼ねる。`docs/plans/active/cross-env.md` の T08） |
| 8 | **`harness gc` に実行経路の検査が無い。** `harness check` の 15 検査にも `tests/*.sh` にも `gc.sh` を走らせる経路が無いため、`gc.sh:42` の `date -d`（GNU 専用。macOS では `illegal option`）が**壊れていても誰も気づかない**。T01 の実装役が「未到達なので直さない」と正しく判断した、その根拠がこの穴そのもの。**2026-09-20 に実証**: macOS で `harness gc` を回すと、日付依存の判定（handoff の鮮度、`active/` plan の放置日数）を**無言でスキップ**し、日付に依存しない 1 件だけ報告した（`date -d` が `|| return 1` で握り潰される）。エラーも警告も出ないので、**gc が仕事の半分をしていないことに気づけない** | gc / テスト | 2026-09-20 | **返済済**（T02 / `f2f8f16`）。`date -d` を GNU/BSD 両対応にし、**読めない日付は WARN**（無言スキップをやめた）。`最終更新:` からの日付抽出も直した。`tests/gc.sh` 7 シナリオを `checks.sh` に fast 登録（修正前 5/6 fail → 7/7 pass、0.7 秒） |
| 9 | `harness init` は mktemp 経由でファイルを書くため、**新規導入直後の perms が docs=0600 / スクリプト=0711** になる（`umask 022` でも）。git は所有者の実行ビットしか記録しないので、今のところ**実害は踏んでいない**（T02 で「踏まなかったものは直さない」に従い未修正） | init | 2026-09-20 | 未着手（低優先。複数人で共有するツリーや、group/other の読み権限が要る配置で表に出たら直す）。**2026-09-21 に実プロジェクト（aesthetic-comparison）への導入で再現を確認**: `.githooks/pre-commit` が `-rwx--x--x`（0711）になった。git は所有者の実行ビットしか記録しないので pre-commit は正常に走っており、**実害は出ていない** |
| 10 | ハッシュの常駐サーバ（`bin/harness` の `hash_server_start`）が、**中断（SIGINT 等）で一時ディレクトリを 1 つ残す**。常駐している `git hash-object` 自体は親の終了で fd が閉じ EOF が届くので残らない（実測で確認済み）。`trap ... EXIT` で掃除できるが、`bin/harness:680` に別の EXIT trap（`HARNESS_REEXEC_TMP`）があり、bash の EXIT ハンドラは 1 つなので統合が要る | CLI | 2026-09-21 | 未着手（実害は `/tmp` に fifo 2 つぶんのディレクトリが残るだけ。trap を統合するときに一緒に直す） |
| 11 | `harness update` が `git hash-object --stdin` を **7 回**呼ぶ（`same_script` / `block_hash`）。うち数回は **`reexec_if_vendored` の再実行で `same_script` が重複して走る**のが主因。ファイル数に比例しないので CS-B1 では対象外とした | CLI | 2026-09-21 | **返済済**（2026-09-21）。`maybe_delegate` の判定を `VENDORED_SAME` で持ち回り、再実行後は `HARNESS_REEXEC` で飛ばす。同じ判定 3 回 → 1 回、`update` は 829 → 793ms。**「効果は小さい」という見立ては当たり**（36ms）だが、同じ回で測った他の 2 つが効いた（`docs/spec/check-speed.md` の B1e） |
| 12 | **`bin/harness` を変えた直後の `harness update` は 1 回では効かない。** `maybe_delegate` が古い同梱コピーへ委譲するため、正本から起動しても走るのは旧版で、新しい `plan()` や配布エントリは次回の update まで効かない（実測 2026-09-21: 配布エントリを 2 行足して update しても `new=0`、2 回目で `new=2`）。`harness check` の "installed copies in sync" は 1 回目で緑になるので気づけない | CLI | 2026-09-21 | **返済済**（2026-09-21、版 0.7.1）。source を解決した後、その `bin/harness` が実行中のものと内容が違えばそちらで実行し直す（`HARNESS_FROM_SOURCE` で往復を止める）。回帰は `tests/update.sh` の U14。**0.7.0 以前から上げるときだけは、これまでどおり 2 回回す**（1 回目で CLI 自身が入れ替わる） |
| 13 | **case を区別しないファイルシステムでは、seed が既存の「大文字違いの同名ファイル」とぶつかると manifest と実体がずれる。** macOS / Windows では `[ -f docs/handoff.md ]` が既存の `docs/HANDOFF.md` にマッチするため seed はスキップされるが、**manifest には `docs/handoff.md` として記録される**。`doctor` の B4（存在チェック）も同じ判定なので素通りする。**case を区別する Linux に持っていくと、B4 が「seed ファイルが無い」と言い、`update` が雛形を配って `HANDOFF.md` と `handoff.md` が併存する**。2026-09-21 に実プロジェクト（aesthetic-comparison）へ初めて導入して発見した（その場では `git mv` で `handoff.md` に寄せて解消した） | init / seed / cross-env | 2026-09-21 | **返済済み**（onboarding-polish T02、2026-09-21）。`harness/scripts/seed-case.sh`（新しい managed ファイル。`bin/harness` の `apply_plan` と `doctor.sh` が共有 source する）に `seed_case_collision()` を切り出し、ファイル名の一致は `find -name` / `-iname`（readdir の生の名前どうしの文字列比較。OS の case フォールディングを経由しない）で見る形にした。`init` / `update` は衝突する seed 行に `CASE-CONFLICT` として何が起きているか・直し方（`git mv`）を出力し、`doctor` は新しい B12 として同じ状態を報告する（case を区別する環境では何も出さない）。**自動リネームはしない**（検出と案内のみ、範囲外のまま）。回帰は `tests/seed-case.sh`（`seed_case_collision()` を単体で呼ぶ。衝突あり/衝突なし/case を区別する環境の 3 系統。実 FS の case 区別はテストから切り替えられないため、判定自体を実 FS 非依存に設計した）。**この返済が対象にしているのは `ownership=seed` のみで、`managed`（`.githooks/pre-commit` 等）は対象外。** 最終レビューの反証役が実機確認（2026-09-21）: 既存に case 違いのファイル（`.githooks/Pre-commit`）を先に置いて `init` すると、seed 分岐の `CASE-CONFLICT` ではなく汎用の `CONFLICT` として `.harness/conflicts/` に逃がされるだけで、#13 由来の `git mv` 案内は出ない。その後 `doctor` は「OK git hooks（... 実行可能）」と**恒久的に緑**を返す（実際に効いているフックは case 違いの無関係な内容のまま）。**managed の case 衝突が未対応で doctor が偽の緑を出す件は #14 として別に起票した** |
| 14 | **#13 の case 衝突検出は `ownership=seed` のみが対象で、`managed`（`.githooks/pre-commit` 等）は対象外。** 既存に case 違いのファイル（例 `.githooks/Pre-commit`）があると、`init` はそれを seed 分岐の `CASE-CONFLICT` ではなく汎用の `CONFLICT` として `.harness/conflicts/` に逃がすだけで、#13 由来の `git mv` 案内は出ない。その後 `doctor` は「OK git hooks（... 実行可能）」と**恒久的に緑**を返し続ける（実際に効いているフックは case 違いの無関係な内容のままで、誰も気づけない） | init / doctor / .githooks | 2026-09-21 | **返済済み**（no-silent-failures T02、2026-09-21）。`seed_case_collision()`（`harness/scripts/seed-case.sh`）は判定そのものが ownership に依存しないため、`doctor` の B12 走査を `managed` にも広げ、同じ判定関数を 1 か所で共有した（複製していない）。説明文・直し方のヘルパーは `seed_case_collision_reason/_fix` から `case_collision_reason/_fix`（第 3 引数 `kind` で seed/managed を出し分け）へ一般化し、`bin/harness` の `apply_plan`（`init`/`update` の `CASE-CONFLICT` 出力）からも同じ関数を呼ぶことで、#13 と #14 の語彙・直し方（`git mv`）を揃えた。重大度は `.githooks/pre-commit` だけ **FAIL**（決定 0007「フックが実行不可なら FAIL」の対象そのもので、case 衝突は「実行不可」よりさらに悪い「harness が中身を保証できない別ファイルが使われる」状態。`harness check` を赤くする経路は決定 0005 の `doctor: FAIL 0` だけなので、WARN のままでは「偽の緑」が残ってしまう）。他の `managed` ファイルの case 衝突は機能欠落の一種として、seed 側の B12（WARN）と同じ扱いに揃えた。実機再現（`mktemp -d`、case を区別しない macOS ボリューム）で確認: 修正前のコード（`48b0386`）では `init` が `CONFLICT`（`CASE-CONFLICT` ではない）を出すだけで `doctor` は `OK=17 WARN=1 FAIL=0`（「OK git hooks（... 実行可能）」を含む）の偽の緑、修正後は同じ状態で `doctor` が `FAIL=1`（`git mv .githooks/Pre-commit .githooks/pre-commit で寄せる` を案内）を返し、`git mv` を適用すると `FAIL=0` に戻ることを確認した。回帰は `tests/doctor.sh` の B12(managed) シナリオ（実 FS の case 区別に依存させず、`fs_case_insensitive()` を差し替えて決定的に再現）。自動でのリネーム・削除はしない（検出と案内のみ。#13 と同じ方針）。**限定（2026-09-21、最終レビュー指摘）**: この返済は B12（managed 向けの新設）が対象で、**B6（git hooks の実行ビット確認）には手を入れていない**。B6 は B12 と独立に判定するため、`.githooks/Pre-commit` のような case 衝突が起きている状態でも「OK git hooks（... 実行可能）」を**今後も出し続ける**（レビュアーが実機で、B6 の OK と B12 の FAIL が同時に出ることを確認した）。`doctor` 全体は B12 の FAIL で赤くなるため実害は無いが、B6 単体だけを見ると誤解する。**次に B6 を触るときはこの前提を踏まえること。** |
| 15 | **`bin/harness` の `hash_server_start`（`init` / `update` / `status` / `diff` が呼ぶ）が、失敗時のエラーを恒久的に握りつぶす。** `mkfifo` の後の `exec 3<>"$d/in" 4<>"$d/out" 2>/dev/null`（`bin/harness:76` 付近）は**コマンドを伴わない `exec`** なので、`2>/dev/null` がその 1 行だけでなく**シェル全体に永続適用**され、以後すべての stderr が `/dev/null` に消える（bash のよくある落とし穴）。この状態で seed 分岐の `mkdir -p` が失敗すると（例: 既存の `docs/SPEC` という**ファイル**があるところへ seed の `docs/spec/` ディレクトリを作ろうとする）、`set -euo pipefail` により `init` / `update` が**エラーメッセージ無しにその場で終了する**（exit=1）。`write_manifest` に到達しないため `.harness/manifest.json` が作られず、`harness status` / `doctor` は「未導入」としか言わない。**中途半端に配布された約 28 ファイルの残骸が残るが、誰も気づけない** | init / update / status / diff（`hash_server_start` を呼ぶ経路すべて） | 2026-09-21 | **返済済み（T01）。** `hash_server_start` と `hash_server_stop` の bare `exec ... 2>/dev/null` を両方とも `{ exec ...; } 2>/dev/null` に包み、fd 2 のリダイレクトをその行の実行中だけに限定した（fd 3/4 の恒久的な差し替えは残るので、常駐に失敗したら黙って 1 件ずつの起動へ落ちる設計は変えていない）。実機再現（既存の `docs/decisions` ファイルが seed の `docs/decisions/` ディレクトリ作成をブロック）で、修正前は exit=1 かつ stderr 0 バイト・`manifest.json` 無し、修正後は同じ exit=1 のまま stderr に `mkdir: ...: File exists` が出ることを確認した。回帰は `tests/hash-server-stderr.sh`（`.harness/checks.sh` に fast 登録）。ハッシュ常駐サーバの高速化（CS-B1）は実測で壊れていない。詳細は CHANGELOG `[Unreleased]` |
| 16 | **最終レビューで「`.claude/settings.json` の case 衝突で deny フックが静かに効かなくなる」（重大度 高）という指摘が出た。`doctor` が沈黙する事実そのものは成立する**: 大文字違いの `.claude/Settings.json` を先に置いて `init` すると `case_conflicts=0` になり B12 の走査対象外になる。`merge_claude_settings`（`bin/harness:609〜661` 付近）は `plan()` / manifest の管理下に無い独立処理で、B12 は manifest エントリしか走査しないため**原理的に検出できない** | Claude アダプタ / doctor | 2026-09-21 | 未着手。**ただし反証で評価が覆った**（onboarding-polish 最終レビューの反証役が実機確認）。報告された実害「deny フックが静かに効かなくなる」は再現しなかった: **case-insensitive（macOS 既定）** では `Settings.json` と `settings.json` が**同一 inode** なので単一ファイルに deny と元の設定が同居し、**deny は有効**。**case-sensitive**（`hdiutil` で APFS 一時ボリュームを作って検証）では、harness が新規作成する厳密一致の小文字 `settings.json` に deny が正しく入り、**むしろ元のプロジェクト設定（allow 等）の方が孤立するという逆向きの実害**だった。**今回の差分由来でもない**: `git archive 61d76dc` で起点のコードを取り出し同じ手順を再現したところ、HEAD と完全に同一の挙動だった（`merge_claude_settings` は今回の差分に一切含まれていない）。**直さない（起票のみ）**。B12 を `merge_claude_settings` の対象にも広げる案は、実害の向きが状況依存（同一 inode か否か）で単純な FAIL 化がかえって誤検知を生みうるため、着手前に実際の被害事例を待つ |
| 17 | **`harness gc` の docs 全体スキャンが plans の規模に対して重い。** T04 で項目 11 の二乗化（spec 1 件ごとに `docs/plans` 全体を `grep -rlF`）を対応表方式に直し、合成データ（spec 300 / plan 1500 / 5.9MB）で **52.5s → 9.2s** になったが、**残り 9.2s は今回の対象外だった既存処理**（節 4 の docs 全体の相対リンク切れチェックなど）に由来する。`gc` の設計前提は「1 秒だから毎回回せる」こと。このハーネスは 1 題材ごとに spec と計画を 1 件ずつ積み上げ、`plans/completed/` を消さない運用なので、規模は単調に増える | gc | 2026-09-22 | 未着手（現在の規模（spec 6 / plan 6）では **0.554s** で実害なし。`gc` が 3 秒を超えたら着手する。T04 と同じ「全体を 1 回走査して対応表を作る」手が使えるはず） |
| 18 | **A の sensor（`gc --commits`、既定 10）は「同一セッション内の書き戻し漏れ」を検出しない。** 閾値は「handoff を最後に触ったコミットより後の非 docs コミット数」なので、**統括が 1〜数コミットの作業をした直後に handoff を更新しそびれる**ケースは素通りする。writeback-sensors の最終レビューで、**この題材の実装セッション自身がまさにそれを再現し、新設した sensor のどちらにも引っかからなかった**ことが実測で確認された（2 体のレビュアーが独立に報告） | gc / task-orchestrate | 2026-09-22 | **返済済み（2026-09-23、[handoff-writeback](spec/handoff-writeback.md)）。** `gc` の項目 12 として、**`docs/plans/active/*.md` を最後に更新したコミットより後に `docs/handoff.md` が更新されていなければ報告する**形にした。判定は `git merge-base --is-ancestor` による**コミットの祖先関係のみ**で、日数・コミット数・mtime のような閾値を一切使わない（起票原因がまさに閾値方式だった）。回帰は `tests/gc.sh` の G29〜G33。**最終レビューが実データで裏を取っている**: 2026-09-22 の実コミット `f093250` の状態を `git worktree` で再現し、新しい項目 12 は WARN を出し、旧来の項目 1（日数・コミット数）は発火しないことを確認した。以下は起票時の記録: 閾値を下げると通常のセッションで警告が出すぎる＝決定ログで 5 を落選させた理由がそのまま効く。**別の角度が要る**: `.harness/state/progress.json` の `phase` と handoff の記述を突き合わせる、あるいは session-handoff を通らずにセッションが終わること自体を検出する。後者は sensor ではなく guide の領域かもしれない）。**`gc` の項目 10 と writeback-sensors の C が「状態の矛盾」を見るのに対し、A だけが「頻度」を見ているのが非対称の原因**。次の題材では、頻度ではなく状態（`progress.json` の `phase` と handoff の記述）の突き合わせから設計し直す |
